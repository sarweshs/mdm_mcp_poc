package com.mdm.mcp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AgentConfig {
    // Agent beans are now auto-detected via @Component annotations
} 